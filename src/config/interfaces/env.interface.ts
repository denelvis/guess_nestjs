export interface EnvironmentVariables {
  NODE_ENV: string;

  APP_PORT: number;

  DB_HOST: string;
  DB_PORT: number;
  DB_PORT_EXPOSE: number;
  DB_USER: string;
  DB_PASSWORD: string;
  DB_NAME: string;
}
